# Stock-Price-Prediction-Using-SVM
Stock Price Prediction Using SVM is a machine learning project that predicts whether a stock price may go up or down using historical market data. It uses Support Vector Machine (SVM) with features like returns, moving averages, and RSI. Built with Streamlit, it shows live prices, charts, and prediction results in an interactive dashboard.
